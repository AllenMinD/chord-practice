# 项目约定

这是本机和弦学习应用，保持本地运行。修改时保留面向零基础学习者的中文说明。

每次修改完成后，检查 `http://127.0.0.1:4173` 的本地服务；若未运行，从本项目目录启动静态服务器（仅绑定 `127.0.0.1`），并验证页面可访问后再交付。服务应以脱离当前命令会话的方式运行，避免会话结束导致服务停止；已运行时不要重复启动。

用户已明确要求：今后所有新增、改编的练习曲都以 Just the Two of Us 的完整练习体验为标准。开始相关工作前阅读 [PRACTICE_SONGS.md](PRACTICE_SONGS.md)，使用共享曲谱模型、歌曲目录和播放器。不要为单首歌曲复制播放器，也不要恢复只有和弦堆叠、随机教学旋律或缺少交互的旧谱例。

新增歌曲必须有可核对的旋律来源、明确的节选和移调说明，并通过该文档的验收要求。没有独立旋律轨时不能把伴奏最高音声称为人声旋律。课程切换时，强制高亮与手动留空必须跟随本课和弦，不能固定为某首歌的某个代号。

验证执行 `npm run check` 和 `npm test`，涉及生成数据时执行 `npm run build:studies` 并确认结果可复现。MIDI 硬件或浏览器未经实际测试时，如实说明验证范围。
