# 所有练习曲的统一标准

用户要求：现有及未来新增练习曲均以 Just the Two of Us 的体验设计。这是新增歌曲的验收标准，不是可选增强项。

## 学习体验

1. 听起来能辨认对应歌曲。选取真实旋律或有代表性的器乐段；保留音高走向、重复音、长短与切分，不用琶音或随机音阶代替。明确标注主歌、副歌或器乐段，不将 MIDI 编配称为原曲录音。
2. 用短节选开始，通常为 4–8 小节。右手旋律与左手简化和弦共用同一份音符数据驱动声音和 MusicXML。提供音名分句、每小节起播、试听单音及谱面下载。
3. 本课和弦自动勾选且不能取消，切课同步更新；其余和弦可手动勾选并按歌曲分别保存。绿色只用于已选和弦及当前和声中的旋律组成音，橙色显示播放位置。未出现的本课和弦必须明确说明，不能替换标记其他和弦。
4. 提供合奏 / 只听旋律 / 只听伴奏、原速 / 慢练 / 更慢、全段 / 前半 / 后半 / 首小节、暂停继续、重头播放和循环。
5. 起播、重播、分段与点小节起播前有一小节 4 拍预备，首拍重音，随当前速度变化。数字、声音与悬浮按钮同步，预备期间谱面不前进。曲中继续与连续循环不重新插入预备拍；预备被中断后重新预备。
6. 默认关闭“手动演奏本课和弦”。开启后只留空本课和弦的左手伴奏，旋律和其他和弦继续；按钮点击同时发出和弦组成音，也支持 Enter / 空格和 MIDI 实际逐音输入，提供缺音 / 多音提示。
7. 演奏按钮离开视野后固定在内容区左侧空白处，原按钮回来即收起。浮动和原位按钮共用输入逻辑、和弦与演奏提示；窄屏改为底部紧凑卡片并避开导航。
8. 暂停、静音、关闭手动模式、切课、切页和页面隐藏必须清理声音、计时器与相应输入状态。MIDI 支持松键、力度、延音、断开设备处理与连接权限失败提示；不发送设备输出。

## 共用实现

- `practice-model.js`：可变小节数的 4/4 曲谱模型，生成 MusicXML、节奏拆分与播放事件；在生成播放事件时过滤本课伴奏，保留旋律。
- `practice-catalog.js`：全部歌曲注册、歌曲名称查询、锚点与默认课程映射。
- `practice-player.js` / `practice-player.css`：唯一播放器与统一交互。保留 `jt-` CSS / DOM 前缀以延续已有样式，不代表只支持某一首歌。
- `chord-instrument.js`：按钮与 MIDI 的实时音色、音符生命周期和设备连接。
- `just-two-data.js` / `just-two-model.js`：原曲数据与配置适配器；`JustTwoStudy` 和 `JustTwoPractice` 保留兼容。
- `practice-song-data.js`：其他歌曲配置与生成的短旋律节选。

## 新增歌曲步骤

1. 核对并保存来源 URL、原文件、SHA-256、PPQ、旋律轨号、源 tick 范围及需要的移调。说明素材实际包含什么；来源下载权限不能被扩展为公开发行授权。本项目保持本机学习用途。
2. 在 `scripts/build-practice-songs.cjs` 添加提取配置，保留逐音的 `sourceTick`、`sourceMidi`、`sourceDuration`、`transpose`、`octaveShift` 与 `bendShift`。相同 MIDI tick 的多声部不能随意拼成旋律，应核对正确主旋律轨。生成冲突时人工核对，不能静默删除后冒充完整旋律。
3. 添加配套 `voicings`（音高、正确音名与 MusicXML 和弦类型）和覆盖整段的 `harmony`。和弦并不一定整小节切换；提前进入、弱起、跨小节与转位简化必须写明。扩展和弦用准确 `kindText` / `degree`，不能只改显示名称。
4. 配置 `id`、`song`、`defaultLesson`、`sectionLabel`、`description`、`tuning`、`sourceLabel`、`simplification`、`sourceNotice` 及 `data`。目录自动为 `PracticeSongData` 中的配置建立共享模型，不需要修改播放器里的歌曲判断。
5. 目录中的歌曲自动出现在选歌入口。优先复用已有和弦课程；`selectLesson(chordId, song)` 分开传递和弦与歌曲，切换到包含同一和弦的歌曲时保留本课，否则选择该曲默认和弦。只有新的和弦类型才在 `app.js` 加课程，并执行 `npm run build:studies`。每首曲子的节选长度和速度由配置提供，不能在播放器写死为 8 小节、96 BPM 或某组和弦。
6. 执行语法、模型与来源回归测试；检查每只手每小节时值相等、延音不产生多余起音、跨和弦的旋律高亮准确。验证本课过滤不会影响另一首歌或旋律，验证来源数据可复现。
7. 验证实际 MusicXML 排谱及播放器状态流程。浏览器或 MIDI 硬件未实测时明确说明。不得把“来源一致”表述为已与原录音逐音比对。

### 公开图片曲谱来源

可使用有明确旋律声部的公开曲谱预览；不得将只有伴奏的最高音当作主旋律。无需为纸谱伪造 MIDI 文件、PPQ 或轨号。

- 保留带署名和水印的原预览图、来源 URL、图片 SHA-256、页号、小节范围和声部。项目仍仅在本机学习使用。
- 在 `scripts/score-excerpts.cjs` 记录逐小节人工核对的音名、时值、休止与延音连线；生成器验证每小节 16 个十六分音符单位、延音的音高及连续性。
- `data.source.format = "score"`；`pages` 记录原图与覆盖的小节，`transcriptionFile` / `transcriptionSha256` 记录转录配置，逐音 `sourceSegments` 保留原小节、拍内位置、音名和时值。生成器合并延音但不合并重复起音。
- 若另配教学和声，必须明确区别于源谱或原录音配法。若没有可靠速度，标为“练习速度”，不能称“原速”。变音名按教学调拼写，例如 A 大调中的 C♯，不随意显示为 D♭。
- 仍须通过相同的模型、排谱、播放器流程与可复现生成验收。

## 当前曲目

| 曲目 | 节选 | 学习和弦 | 来源与处理 |
| --- | --- | --- | --- |
| Just the Two of Us | 萨克斯 8 小节 | D♭maj7、C7；这段无 Fm7 | MIDIdb Demo，保留萨克斯旋律，后半段提高一个八度 |
| Imagine | 主歌 8 小节 | Cmaj7 | BitMidi MIDI 的 harmonica 主旋律轨；左手 C–Cmaj7–F，Cmaj7 占每两小节中第一小节的最后一拍 |
| Wonderwall | 主歌 8 小节 | Em7、Dsus4、Cadd9 | MIDIdb Demo 的 oboe 主旋律轨，整体降低两个半音对应本课键位，扫弦改为紧凑和弦 |
| Dreams | 副歌 8 小节 | Fmaj7 | Midis101 的 Melody / flute 轨，旋律整体提高一个八度，左手 Fmaj7–G |

旧 Imagine 与 Dreams 的 MIDI Demo 只作来源存档；它们缺少独立人声旋律，不再作为当前主旋律数据。旧原创教学旋律与独立 Imagine 播放器已由共享练习替换。

| 新增中文曲目 | 节选 | 学习和弦 | 来源与处理 |
| --- | --- | --- | --- |
| 晴天 · 周杰伦 | 主歌唱名短句 4 小节 | Em7、Cmaj7 | 人人钢琴网 Jeanie 原调简单版第 13–16 小节；不移调，68 BPM 为谱面速度；左手另配 Em7–Cmaj7–G 教学和声 |
| 遇见 · 孙燕姿 | 主歌短句 4 小节 | Fmaj7、Em7 | 人人钢琴网 lcz_0205 精修谱第 13–16 小节；A♭ 大调升 4 半音至 C 大调，保留延音和休止；另配 Fmaj7–G–Em7–Am |
| 爱爱爱 · 方大同 | 主歌开头 4 小节 | Em7 | 人人钢琴网五线谱第 10–13 小节带歌词声部；保留 A 大调音高，省去倚音与内声部；按源谱 A–Em7–A–D–C♯m–Bm 简化，88 BPM 为练习设置 |
